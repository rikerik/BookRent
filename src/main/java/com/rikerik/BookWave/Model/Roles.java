package com.rikerik.BookWave.Model;

//Roles for the user
/**
 * The Roles enum represents the different roles that a user can have in the system.
 * It includes the USER role and the ADMIN role.
 */
public enum Roles {
    USER,
    ADMIN
}
